---
name: <skill-name>
description: >
  <做什么：一句话说明核心动作与产物。何时触发：列出用户场景与触发词，收集用户原话和
  口语化表述。倾向 pushy 以对抗欠触发，例如“即使没有明确说 X，只要涉及 Y 都应使用”。
  负向边界可选：仅当实际发生过误触发或误用时才写，不写推测性禁止。整体 ≤1024 字符。>
---

# <Skill Title>

<一到两句定位：这是什么、核心动作、产出什么。如果技能有多种使用模式，在这里概述。>

## When to use

- <触发场景一：用户带着什么输入、想达到什么结果>
- <触发场景二>
- <与相邻技能的分工：本技能负责哪一段>

## When not to use

- <只按事实写：实际发生过误路由或误用，才写对应边界；不预先写推测性的禁止>

## Inputs / Outputs

**Inputs:**
- <必需输入及其常见形态（路径、格式）>
- <可选输入>

**Outputs:**
- <产物及其位置、格式>
- <Canonical output：默认产物、唯一来源、状态出口和验证方式；需要升级时说明触发条件>

## Workflow

### STEP 1: <动作名，英文动词短语>

<第一步建议是“探索背景与边界、确认意图”：一次只问一个问题，问题带上自己的理解作为
选项供用户选择，意图与边界清楚后再动手。控制标记用英文（IF / THEN / STOP / PAUSE），
正文用自然中文；与用户交流的语气口语化、有温度。>

STOP when <可自检的完成条件——用操作性测试，不用无法度量的阈值>。

### STEP 2: <下一个动作>

<涉及用户决策的步骤，按"呈现 → 等待确认 → 分流"组织：>

IF 用户同意，THEN <执行动作>。
IF 用户提出反馈，THEN 先判断反馈是否改变意图或方向：若是，回退到 `STEP N`；否则就地更新。

<关卡写成编号检查清单，每条带 fallback：>

1. <检查项一>？IF 否，THEN <回到哪、做什么>。
2. <检查项二>？IF 否，THEN <回到哪、做什么>。

<先用正向契约定义默认产物、来源、状态出口和升级条件；只有有证据的误用才加入最小负向
guardrail，并同时写出替代行为和 fallback。>

<!-- 生成或修改供人阅读文档的 skill 保留本步骤；其他 skill 删除。 -->

### STEP N: Validate generated documents

重新读取实际写入的文件，并执行文档语言关卡：

1. <正文是否自然、具体；不清楚时如何改写>。
2. <必须保持不变的事实、决策、状态、ID、路径、命令、引用和技术术语>。
3. <语言修改不得改变的需求、风险、契约或其他行为含义>。
4. <语言修改后重新运行哪些结构、追踪关系、状态和格式检查>。

## Verification

- [ ] <产物存在的客观检查>
- [ ] <产物结构/格式检查>
- [ ] <行为边界检查：什么未被修改>
- [ ] <流程终态检查：以哪个动作/推荐结束>
- [ ] <产出型 skill：已重新读取实际文件并通过文档语言关卡>
- [ ] <产出型 skill：语言修改没有改变事实、决策、状态、ID 和技术含义>

## Safety & guardrails

- <只读/写入边界声明>
- <昂贵动作的确认要求>
- <来源核对规则：重新读取原始资料并据此填写，不凭记忆补写或编造>
- <持续执行与简化条件：简单场景允许压缩流程>

## References

- <references/xxx.md> — <一句话说明内容 + 何时读>
- <assets/xxx.md> — <一句话说明用途>
