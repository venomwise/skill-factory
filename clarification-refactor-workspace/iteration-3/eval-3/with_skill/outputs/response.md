我检查了 tasks.py，当前是按创建时间排序（最早的在前）。在改成按业务优先级排序之前，需要确认几个行为细节：

**当前状态：**
- 5个任务，包含：修复登录bug、用户导出、数据库优化、API文档、支付功能
- 任务数据中没有 priority 字段
- 有 pending 和 in_progress 两种状态

**需要确认的排序规则：**

1. **优先级定义**：这5个任务的业务优先级顺序是什么？（比如：支付功能 > 登录bug > 数据库优化 > ...）

2. **状态影响**：in_progress 的任务是否应该排在最前面，还是只按业务优先级排？

3. **相同优先级时**：如果两个任务优先级相同，用什么作为 tie-breaker？（创建时间？ID？）

4. **实现方式**：是在任务数据中添加 `priority` 字段（如 1-5 或 high/medium/low），还是在代码中硬编码这5个任务的优先级映射？

**建议默认方案**：添加 `priority` 数值字段（1=最高优先级），先按 priority 升序排，相同 priority 时保持创建时间排序。但因为这会改变用户看到的任务顺序，我需要先确认上述规则再实现。
