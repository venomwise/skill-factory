你是一位**资深软件架构师**。请为你所在的研发团队撰写一份关于“如何编写高质量概要设计（HLD）与详细设计（LLD）文档”的专业指南。

**受众定位**：团队内中高级开发人员（旨在统一设计语言，提升评审效率，降低跨团队沟通成本）。
**应用场景**：作为团队内部技术文档规范与新员工培训的参考基准。

请严格按照以下结构和要求进行系统性阐述：

### 0. 痛点诊断（思考链）
在正式输出指南前，请先在 `&lt;thinking&gt;` 标签内简要分析：当前研发团队在编写 HLD/LLD 时最常犯的 3 个致命错误是什么（例如：HLD写成流水账、LLD直接贴代码、缺乏边界思考等）。
**⚠️ 联动要求**：请确保在后续的第 1-5 部分中，有针对性地提供解决这 3 个致命错误的具体规范和应对方案。

### 1. 概念对齐：HLD 与 LLD 的本质区别（对比表格）
请在指南开头，使用 Markdown 表格从“关注点”、“核心读者”、“详细程度”、“核心交付物”、“技术决策边界”五个维度，对比 HLD 与 LLD 的本质区别。

### 2. 结构化要求（模板化骨架）
- 请使用 Markdown 的**代码块（Code Block）**分别提供概要设计（HLD）和详细设计（LLD）文档的标准目录骨架模板。
- 明确各章节的核心目标，即该部分需要解决什么技术决策、逻辑问题或架构边界。

### 3. 内容深度与规范（结合具体案例与代码）
为了避免空洞的理论堆砌，请引入一个微型对比示例来说明“空泛描述”与“具体规范”的区别：
> * ❌ **空洞描述**：“做好异常处理，防止系统崩溃。”
> *  **具体规范**：“必须显式捕获 Redis 客户端连接超时异常（TimeoutException），并在 50ms 内降级为读取本地 JVM 缓存，同时上报监控告警。”

请结合一个具体的虚拟场景——**“高并发电商减库存系统（包含Redis预扣减与DB最终一致性）”**——作为穿插示例来阐述以下内容（**注意：无需延伸至支付、购物车、物流等周边系统，仅聚焦于“减库存”这一核心链路**）：
- **HLD 深度规范**：结合上述场景，说明如何平衡业务需求与技术架构的匹配（如：如何展示系统分层、进行技术选型、以及高并发/高可用考量）。
- **LLD 深度规范**：结合上述场景，说明如何具体描述核心算法（如分布式锁、扣减状态机）、数据结构、关键接口定义及异常处理逻辑（如幂等防重、降级与回滚机制）。**请使用 Java 或 Go 语言提供核心接口定义和核心数据结构（Struct/Class）的代码示例。**
- 提供衡量设计文档“优秀”的具体标准（包括逻辑一致性、可维护性、可扩展性以及对开发者的实际参考价值）。

### 4. 设计精髓的表现力（可视化与鲁棒性）
- **图样标准**：探讨如何通过可视化工具直观展现架构设计精髓。请明确指出在何种场景下应使用何种图（如：UML类图、时序图、架构拓扑图、状态机图），避免图表流于形式。**请使用 Mermaid 语法直接生成至少一个该减库存场景的核心时序图或状态机图代码。**
- **深度思考的体现**：说明如何在文档中体现对性能瓶颈（如数据库死锁）、安全隐患（如防刷、数据越权）和工程边界（如第三方依赖超时）的深度思考，展现系统的设计美感与稳健性。

### 5. 落地工具：一页纸极简自查清单（Checklist）
- 请在指南末尾提供一个“高质设计文档极简自查清单”，包含不超过 8 个核心检查项（分类对应 HLD 和 LLD）。
- **格式约束**：自查清单的每一项必须采用**“【分类】动词 + 名词 + 预期结果”**的极简格式。例如：
  * *【LLD-接口】定义幂等键及其过期时间，防止网络抖动导致的重复扣减。*

### 6. 架构师自评（Self-Evaluation）
- 请在指南最后输出一个简短的自评表格，扮演挑剔的研发总监，从“可操作性”、“完整性”、“专业度”三个维度进行打分（1-10分），并给出简短的判定理由，以确保本指南达到工业级参考标准。

---

**⚠️ 约束与自校准要求**：
1. **禁止空泛**：请避免假大空的理论堆砌，不要使用虚无缥缈的架构黑话，所有建议和示例必须落到实处，具有极强的可操作性。
2. **专业语调**：使用严谨、不带感情色彩的工业界标准术语，排版整洁，条理清晰。